{
  "name": "@my-travel-journal/server",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "test": "jest --coverage",
    "test:update": "jest --updateSnapshot",
    "clean": "rimraf dist",
    "build": "yarn clean && tsc",
    "start": "yarn build && node ."
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/morgan": "^1.9.10"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "log4js": "^6.9.1",
    "morgan": "^1.10.1"
  }
}
